# Cleanup candidates (не удалено — требует решения)

Дата: 2026-05-22. Политика: при сомнении **не** удаляем из репозитория; фиксируем здесь.

## Удалено в этом спринте (подтверждено мёртвым)

| Объект | Причина |
|---------|---------|
| `components/DemoNotice.tsx` | Нигде не импортировался; README/структура устарели. |

## Код

| Кандидат | Почему не трогали |
|----------|-------------------|
| `lib/admin/safe-return-to.ts` | Нет импортов в `.ts`/`.tsx` (на момент аудита). Open-redirect защита; вероятно задумано для `returnTo` после логина, но сейчас `proxy.ts` использует параметр `redirect`. Либо подключить к формам логина/навигации, либо удалить после явного решения. |
| `package.json`: `@tanstack/react-query`, `shadcn` | Нет прямых импортов в исходниках приложения. Могут использоваться CLI/будущими экранами или остаться «на вырост». Удаление зависимостей — отдельный PR с `npm run build`. |

## Документация

| Кандидат | Почему не трогали |
|----------|-------------------|
| `admin-public-content-map.md` (в корне `mansvalve/`) | Отдельный русскоязычный аудит (таблица по секциям). В `docs/admin-public-content-map.md` — другая структура (ADMIN/PUBLIC/ACTUAL). Содержимое не полностью совпадает; объединение в один файл — вручную, чтобы не потерять строки. |

## Маршруты и legacy

Следующие вещи **намеренно не** считаются мёртвым кодом (требования спринта):

- Legacy `GET /catalog/[slug]` для товара → редирект на канонический URL.
- `lib/public-catalog/slug-aliases.ts` и таблица алиасов в БД.
- JSON-каталог и fallback при `PUBLIC_CATALOG_SOURCE=json`.
